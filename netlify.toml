[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 3000
  framework = "#custom"

# SPA fallback（必須放最後）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
